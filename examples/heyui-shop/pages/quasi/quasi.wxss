/* pages/quasi/quasi.wxss */
page{
  /* rgba的四个值分别为：红(R)、绿(G)、蓝(B)、透明度(A) */
  --whiteShadow:  -6px 6px 12px #d3473d;
  --blackShadow: 6px -6px 12px #f75347;
}

.left{
  border-radius: 42px;
  background: #d3473d;
  box-shadow: var(--blackShadow),
      var(--whiteShadow);
}